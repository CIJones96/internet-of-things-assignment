CREATE TABLE `i7467340`.`smart` (
  `smart_id` INT NOT NULL,
  `smart_name` VARCHAR(45) NOT NULL,
  `smart_info` VARCHAR(45) NOT NULL,
  `smart_energy` DECIMAL(5,2) NOT NULL,
  `smart_time` TIME NOT NULL,
  PRIMARY KEY (`smart_id`));
